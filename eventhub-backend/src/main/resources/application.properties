# ===============================
# SERVER CONFIG
# ===============================
server.port=${PORT:5000}
# Matches your Vercel/Vite /api bridge
server.servlet.context-path=/api

# ===============================
# DATABASE CONFIG (Neon Postgres)
# ===============================
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-wandering-leaf-a1ml8tap-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_XQYJlj8oEyA1}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.idle-timeout=120000
spring.datasource.hikari.connection-timeout=20000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.default_batch_fetch_size=100
spring.jpa.open-in-view=false

spring.jpa.properties.hibernate.format_sql=true
# Fixes PageImpl serialization issues (empty JSON objects)
spring.data.web.pageable.serialization-mode=via-dto

# Lazy initialization disabled — ensures all beans are created at startup
# so errors surface immediately rather than as 500s on first request
spring.main.lazy-initialization=false
# ===============================
# JPA CONFIG
# ===============================

# ===============================
# APP CONFIG
# ===============================
app.file-upload.dir=${UPLOAD_DIR:./uploads}
app.scheduled.reminder-cron=0 * * * * *

# ===============================
# JWT CONFIG
# ===============================
jwt.secret=${JWT_SECRET:eventhubsupersecretkeychangeinproductionmustbe256bitslongrmfrnfjefiwjefiwuheiuhwcuncunwnc}
jwt.expiration=${JWT_EXPIRATION:2400000}


# Fixed syntax: Choose the Vercel URL as the primary default
app.frontend-url=${FRONTEND_URL:https://event-hub-beta-cyan.vercel.app}
# ===============================
# CLOUDINARY CONFIG
# ===============================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:duperqbxd}
cloudinary.api-key=${CLOUDINARY_API_KEY:946223754777951}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:CzWfrGCoGAyUqLLsi3cHuKRsQ5E}

app.mail.from-email=${MAIL_FROM:EventHub <event.hub.official.main@gmail.com>}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:event.hub.official.main@gmail.com}
spring.mail.password=${MAIL_PASSWORD:tnqv xlmm rbnv xvlp}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false
