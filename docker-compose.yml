version: '3.8'

services:
  backend:
    build:
      context: ./eventhub-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    restart: unless-stopped

  frontend:
    build:
      context: ./EventHub_Frontend
    ports:
      - "80:80"
    restart: unless-stopped
